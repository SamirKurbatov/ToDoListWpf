<Window x:Class="ToDoList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoList"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="800">
    <Window.Resources>
        <ResourceDictionary Source="Resources/Colors.xaml"/>
    </Window.Resources>
    <Border Padding="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Top" Orientation="Horizontal">
                <TextBox x:Name="noteTextBox" Height="30" Width="200" Text="{Binding MainNote.Title}"/>
                <Button Content="Добавить" x:Name="addButton" Padding="5" HorizontalAlignment="Right" 
                        Command="{Binding AddCommand}" Background="{StaticResource greenButtonColor}"/>
            </StackPanel>

            <GroupBox Grid.Column="1" 
                      Header="Задачи" 
                      BorderThickness="2" 
                      Margin="2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <ListView x:Name="noteListView" 
                              Grid.Row="0"
                              ItemsSource="{Binding Notes}" SelectedItem="{Binding SelectedNote}" Height="Auto">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="2">
                                    <Grid Margin="2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition/>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>
                                        <StackPanel Grid.Row="0">
                                            <TextBlock FontSize="12" FontWeight="Bold" Text="Id"/>
                                            <TextBlock FontSize="18" Text="{Binding Path=Id}"/>
                                            <TextBlock FontSize="12" FontWeight="Bold" Text="Title"/>
                                            <TextBlock FontSize="18" Text="{Binding Path=Title}"/>
                                            <TextBlock FontSize="12" FontWeight="Bold" Text="Date"/>
                                            <TextBlock FontSize="18" Text="{Binding Path=Date}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <WrapPanel Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Center">
                        <Button Background="{StaticResource redButtonColor}" 
                                                    Content="Удалить" 
                                                    x:Name="removeButton"
                                                    Padding="5" HorizontalAlignment="Right" 
                                                    Margin="0 10 0 0" 
                                                    Command="{Binding RemoveCommand}" CommandParameter="{Binding}"/>
                        <Button Background="{StaticResource gradientButtonColor}" 
                                                    Content="Редактировать"
                                                    x:Name="editButton" 
                                                    Padding="5" 
                                                    HorizontalAlignment="Right" Margin="0 10 0 0" Command="{Binding OpenCommand}"/>
                    </WrapPanel>
                </Grid>
            </GroupBox>
        </Grid>
    </Border>
</Window>
