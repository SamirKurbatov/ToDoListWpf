<Window x:Class="ToDoList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoList"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <ResourceDictionary Source="Resources/ButtonColors.xaml"/>
    </Window.Resources>

    <Window.CommandBindings>
        <CommandBinding Command="local:CrudCommands.AddCommand" Executed="AddCommand_Executed"/>
        <CommandBinding Command="local:CrudCommands.OpenCommand" Executed="OpenCommand_Executed"/>
        <CommandBinding Command="local:CrudCommands.RemoveCommand" Executed="RemoveCommand_Executed"/>
    </Window.CommandBindings>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical" 
                    HorizontalAlignment="Left" 
                    VerticalAlignment="Top" 
                    Margin="10 10 0 0" Grid.Column="0">
            <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                <TextBox x:Name="noteTextBox" Height="30" Width="200"/>
                <Button Content="Добавить" x:Name="addButton" Padding="5" HorizontalAlignment="Right" 
                        Command="local:CrudCommands.AddCommand" Background="{StaticResource addButtonColor}">
                </Button>
            </StackPanel>
        </StackPanel>

        <ListBox x:Name="noteListView" Grid.Column="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock FontSize="16" TextWrapping="Wrap">
                            <Run Text="Id: "/>
                            <Run Text="{Binding Id}"/>
                            <Run Text=" Название: "/>
                            <Run Text="{Binding Title}"/>
                            <Run Text=" Дата и время: "/>
                            <Run Text="{Binding Date}"/>
                        </TextBlock>
                        <WrapPanel HorizontalAlignment="Right">
                            <Button Background="{StaticResource removeButtonColor}" 
                                    Content="Удалить" 
                                    x:Name="removeButton"
                                    Padding="5" HorizontalAlignment="Right" 
                                    Margin="0 10 0 0" Command="local:CrudCommands.RemoveCommand"/>
                            <Button Background="{StaticResource gradientButtonColor}" 
                                    Content="Редактировать" 
                                    x:Name="editButton" 
                                    Padding="5" 
                                    HorizontalAlignment="Right" Margin="0 10 0 0" Command="local:CrudCommands.OpenCommand"/>
                        </WrapPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
